{
  "name": "root",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "npm run server --prefix server",
    "client": "npm run start --prefix client",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "install-all-windows": "(npm install) && (cd client && npm install) && (cd ../server && npm install)",
    "install-all-mac": "(npm install) ; (cd client && npm install) ; (cd ../server && npm install)"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Mdemenko1/inventory.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Mdemenko1/inventory/issues"
  },
  "homepage": "https://github.com/Mdemenko1/inventory#readme",
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
